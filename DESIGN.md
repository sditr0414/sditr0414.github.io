# 포트폴리오 디자인 기준 · v9

## 이번 변경

v8의 디자인을 유지하고 3·10페이지의 문구만 조정했습니다. 이동형 로봇의 제작 기간은 제목과 하단 정보에서 강조하며, 제목 크기를 키우거나 새로운 배경 상자를 추가하지 않습니다. 졸업논문은 본인 역할만 설명하되 공동 연구 표기는 유지합니다. `styles.css`, `app.js`, 원본 이미지·영상은 v8과 동일합니다.

## 표지 색상

| 역할 | 색상 |
| --- | --- |
| 웹 바깥 배경 · 기존 유지 | `#e7eef8` |
| 1페이지 바탕 | `#24476b` |
| 우측 사진 영역 | `#1b3552` |
| 사진 프레임 | `#345779` |
| 이름 | `#ffffff` |
| 소개 문장 | `#f0f6ff` |
| 본문 설명 | `#d3dfef` |
| 기술 목록 | `#dce7f6` |
| 보조 정보 | `#bdcfe5` |
| 구분선 | `#526f8f` |

규칙은 `styles.css` 마지막 v8 구간의 `.cover`에만 적용한다. 인쇄용 복제 페이지에는 ID가 없으므로 `#profile`이 아니라 `.cover`를 사용한다. 사진과 캡처의 색상은 변경하지 않는다. 그라디언트나 새 장식은 추가하지 않았다.

## 공통 화면과 출력

- 웹과 PDF의 원본은 1120×630px, 16:9의 같은 `.slide`이다.
- 글자 크기, 줄바꿈, 이미지 배치는 변경하지 않는다.
- A4 세로 한 장에 같은 슬라이드 두 개를 비례 축소해 배치한다.
- 모바일의 세로 배치는 유지하며, PDF는 데스크톱 원본을 기준으로 한다.
- 출력은 설치된 Noto Sans CJK KR로 확인했다. 폰트 파일은 배포하지 않는다.
